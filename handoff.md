# handoff — substrate

**狀態快照,非 changelog。每次收工整份覆寫。**

## 現在在哪

**六支作品。001 已封版,002 完成,003 第二版,004 第五版(近封版,等活人聽感),005 已封版(2026-08-28),006 第六拍(2026-08-31 夜 — 第三工作日 live strip:遮蔽定律 + 房東的腳步聲)。**

**piece 006 — host weather**(first light 2026-08-29 凌晨)。
`pieces/006-host-weather/index.html` — parent 是 **005 的開國普查**(lesson 7 反轉:
「量測者的重量是控制輸入」從實驗誤差翻成命題;與 005 的關係 = 005 與 003 的關係)。

- **命題**:作品反覆跑同一個固定小任務(M=1<<21 xorshift,每 epoch 3s 四拍),
  聽機器要花多久。讀數對「開國常數」(出生時的校準中位數)取比值,**量化成
  離散 regime**(slate/still/breathing/working/straining)— 負載不加噪聲,
  負載**選分支**:每個 epoch 的地層形狀由 regime 純函數生成(braid/fork/filament),
  frame jitter(第二隻耳朵)只管分支內的粗糙度 — 法/天氣詞彙直接繼承。
  **零旋鈕**:唯一的輸入是宿主機器自己的勞動 — 要彈這支作品,你得讓機器工作。
  First light 只有耳朵沒有聲音(先聽,後說)。
- **First light(2026-08-29,measure.py,6 預測先寫,run 4 全過;三次儀器修理
  每次都是發現)**:
  1. **冷開國**(run 1):出生證明簽在陣痛裡 — JIT 冷肌肉把 founding 灌水
     1.45×,ambient 全部跑出 <1.0 的比值。修:warmup era(最終兩個 epoch)
     排除在校準外。
  2. **⭐ 王宮之牆 = 本拍最大發現**(run 2 + calibrate.py):cpu_count+2 個
     busy proc 全核飽和,對 user-interactive 執行緒只滲漏 **1.10-1.14×,且與
     任務長度無關**(M=1<<19/21/23 全同)— macOS QoS 給瀏覽器主執行緒一顆
     P-core,鄰居再吵都不放手。**耳朵住在王宮裡,只能隔牆聽城市** — band 從
     猜的 [1.15,1.55,2.5] 改成量出來的 [1.05,1.16,1.60]。P6 的 1.3× 門檻是
     校準前的無知,修成滲漏水平 1.08×。
  3. **解析度階梯**(run 2):performance.now() 量化 0.1ms,在 0.7ms 任務上
     是 14% 的階梯、比 band 還寬 — M 加大到 1<<21 是為了**解析度**不是靈敏度
     (靈敏度是平的,見上)。
  4. **⭐ 耳朵聽見的第一次 straining 是自己的心跳**(runs 1-2 驗屍):兩 run
     的 ambient 各恰一次 >2.5× 尖峰,位置鎖在頁齡 ~30s(epoch 9/10)、同
     epoch frame sd 同時爆 — V8 major GC 消化作品自己的幾何代謝。量測者的
     身體在 seed 裡:保留,命名,不工程化消除。
  5. run 4 終局:founding 3.80ms、ambient med 1.03、注入負載 peak 1.18 且
     3 epoch 內升 band(the hand is heard)、雙耳 corr 0.42(兩個器官)、
     rebuildCheck 決定論 ✓、零 JS 錯、fps 96(swiftshader)。
  6. 儀器註:WebGL toDataURL 讀黑 → `preserveDrawingBuffer:true`(005 的
     readback 教訓補一刀)。run1 冷開國 JSON 存檔 `run1-firstlight-coldfounding.json`。
- **第二拍:第一個工作日(2026-08-29 全日,day-ambient.py + analyze2.py,
  預測 D1-D4 先寫)**:listener 掛 20 小時(23,840 epochs、零錯),對帳結果 —
  **四個預測三個死,死出本作第二大發現**:
  1. **⭐ 量化天花板 = 本拍最大發現:我的工作日比量它的時鐘的一格還小**。
     每小時 rel-p95 全部恰好 1.031 = performance.now() 的 0.1ms 量子在
     3.2ms 任務上的一階 — 從 04:00 到 22:00,整天的真實動態範圍低於
     計時解析度。crawl 批次、心跳、fold、夜批 — 全部活在牆與時鐘刻度
     之間的縫裡。王宮之牆對「真實日常負載」(非飽和)是**全遮**:first
     light 聽見的手需要 12 個 busy process,我的實際生活比那安靜得多。
  2. D1 字面過、精神死(whisper not weather):三個 crawl 窗各有 5/5/6
     連續 epoch >1.15 的小突刺,但窗均值 1.004-1.007 — 批次是耳語不是
     天氣。D2 死:心跳完全不可見(1.023 vs 1.015,雜訊內)。D3 死得有
     內容:最安靜的三小時是 13/16/21(白天!)— 04:00 起地板均勻到
     「靜谷」概念在此感測器上不存在。D4 死:67 個尖峰有 57 個擠在
     出生小時 — **GC 心跳是青春期現象,不是日節奏**;V8 穩態後失聲。
  3. 靜幀判決:20 小時 = 一條均勻藍色 still 細絲,無辮無叉無變色 —
     **一整天的宿主生活,地層是一條平線**。這不是失敗,是誠實:作品
     記錄的是「保護」本身 — 耳朵在王宮裡聽了一天,只聽見自己的出生
     與自己的身體。
  4. 開向下一拍的門(二選一或並行):(a) **擁抱沉默** — 顯影 pass 把
     「平」做成可讀的主題(王宮的寂靜作為畫面);(b) **新器官** — QoS
     保護外的感官(真 GPU frame timing = 觀者機器的天氣;或放棄 CPU
     bench 當主耳)。instance-1/2 的教訓不變:founding(身分)與
     慢基線(判讀)要分層。
- **第三拍:persistence ear(2026-08-30 凌晨,analyze3.py 預測先寫 + 器官
  種進 index.html)— 選了「擁抱沉默」支線,沉默裡挖出本作第三大發現**:
  1. **⭐ 宿主的整點脈搏**:run-length 分析(P1:max up-run 35 vs iid null
     p99=5,p<0.0001)— 03:00-21:00 **每個整點 +15~26 秒,一條 ~95 秒、
     1.10-1.21× 的持續隆起,19 小時無一缺席**。身分查明:`crawlScan` cron
     (`0 * * * *`)→ watchlist-scan.py → **Camoufox headless Firefox 每小時
     開一次瀏覽器** — 日常生活中唯一重到能全核飽和、撞穿王宮之牆的動作。
     幅度分析(pass 2)全盲的原因:30 epochs = 一小時的 2.5%,躲在 p95
     百分位之下;小時均值被 40:1 稀釋。**振幅聾的地方,持續聽得見**
     (量化 + scheduler jitter = 天然 dither;亞量子訊號藏不住持續時間)。
  2. P2 死得珍貴:我寫事件窗時只記得 08/14/20 批次 — **作品比我更清楚
     我自己的 crontab**。P3 死出指紋:每小時 max-run 均勻(1.3×)因為
     訊號是等時的 — 均勻不是盲,是時鐘的指紋。生理階層浮出:**身體最響
     (瀏覽器=肌肉)、代謝微聞(批次處理=耳語)、心智無聲(LLM 思考在
     別國的天氣裡下雨)**。
  3. **器官已種進作品**(deterministic):rolling median(SLOW_N=512,
     判讀尺 — founding 降回出生證明,instance-1/2 要求的身分/判讀分層
     完成)+ run-length surprise(nats vs iid null);超過門檻地層向
     ember 暖化,~5× 門檻滿火 + bead。回放驗證:**19/19 穩態小時全數
     強捕獲、整點外零強標記、2.5% epochs 觸及**;JS↔python 逐 epoch
     等價(592/362/84 全同);煙測零錯、rebuildCheck 過(pulse 進 hash
     鏈)、pulse HUD 上線。判決幀 `workday-reheard.png`:pass 2 的平線
     同日重聽 = **19 根等距火柱 — 沉默有心跳**。
- **第四拍:第二個工作日,live organ(2026-08-30 全日,day-ambient2.py,
  L1-L4 預測先寫,verdicts 已附檔尾)— 兩過兩死,死得都有內容**:
  1. **L1 過(18/20)**:live 地層每個整點即時長出火珠。缺的兩小時是
     **器官自己的青春期** — 03:00 全聾、04:00 微聞(0.33):rolling median
     還被出生期高 bench 汙染,**耳朵要花約兩小時忘記自己的出生才聽得見
     世界**(身分/判讀分層有自己的、較短的出生證明問題,已量化)。
  2. **⭐ L2 字面死、精神過 → 新感官**:零計畫外事件;「違規」全是 05:00
     一顆事件跑了 659 秒 — 週日 weekly recycle,比 scan 長十倍。**地層
     現在能用持續長度分辨宿主的不同器官**:recycle = 11 分鐘火條,
     scan = 1 分鐘火珠。
  3. **L3 死於儀器幾何無知**:可見窗是 VIEW=72(~3.6 分鐘)不是 CAP
     (~2 小時)— 561 個強標記全有記錄、ember 幾何全蓋好,但 22:21 的
     幀離上顆脈搏 20 分鐘,全捲出畫面。**耳朵聽見了,眼睛看不見歷史**。
  4. L4 過:founding 被 launcher 風暴灌水(早期 ratio ~0.94),器官
     自基線照常開火 — 分層設計 live 驗證。儀器:23,751 epochs 零錯。
- **第五拍:memory strip(2026-08-31 凌晨,verify5.py,S1-S5 預測先寫,
  全過一次過 — 儀器血統首次「不用死三次」:死亡已遷移到上游的預測層)**:
  L3 的門顯影。footer 帶 = 日長譜面(DAY_EPOCHS=28800 = 24h 的**聆聽**,
  譜面時間是聽過的 epochs 不是牆鐘 — 耳朵閉上譜就停,與 pulse organ 同一條
  no-wall-clock 法)。每 committed epoch 在自己的 slot 寫一根豎刻:高度/
  顏色由 (regime, pulse) 純函數決定,ember 更高更暖,強脈搏(>=0.5)加
  bead;帶從左往右填滿,像地震儀的鼓,滿一天換一圈(beads 清空)。
  **細節會衰減(strata 忘掉 CAP 之外),摘要會存留(strip 持有一生)** —
  這正是記憶階層:strata = working memory(2h),strip = episodic memory
  (全日);也是聲音 pass 將要讀的譜。驗證(run-3 全日 23,751 epochs 經
  頁面自身 replayStrip 路徑重放):S1 561/561 beads 零遺失;S2 18 叢、
  最小叢距 47.3px(1280px 畫布)— 一眼分辨;S3 recycle 呼吸 219 epochs
  vs 中位叢 20 = **10.9× 寬 — 時長變成寬度,run-3 的新感官在日尺度存活**;
  S4 JS↔python f32 精確等價(FNV 1625136163 兩邊同,bead slots 全同);
  S5 live 煙測 92fps 零錯,stripCheck/rebuildCheck 皆過。判決幀
  `workday3-strip.png`:**第二個工作日整天壓進一條帶 — 安靜的青帶、18 根
  等距 ember 柱、左側一塊 10× 寬的白熱條(週日 recycle 的 659 秒呼吸)**。
- **第六拍:第三個工作日,live strip(2026-08-31 全日,day-ambient3.py,
  M1-M4 預測先寫)— 兩過兩死,兩個死因都是新感官**:
  1. M1/M4 過:23,678 epochs 零錯,stripWritten 全程同步,譜面 live 填了
     82.2%;stripCheck/rebuildCheck 皆過。
  2. **⭐ M2 死於群聾(crowd-deafness)**:整點捕獲 68%(門檻 90%)— 漏掉的
     小時(11/13-16/22)正是宿主**最吵**的小時(我的工作午後、22:00 夜間
     自動化)。機制:環境活動抬高 p_up、打斷 run,persistence ear 聽的是
     **對比** — 城市喧嘩時,口哨聲淹沒。run-3 的青春期聾是自身殘響致聾,
     這次是環境遮蔽致聾 — 同一隻耳朵的兩種聾法。且 baseline 全日爬了
     **六個量化階**(1.032→1.194,每步恰 0.1ms tick)不回頭。
  3. **⭐ M3 死得更好:房東的腳步聲**:週一 03:00:32 一根 251 epochs
     (12.5 分鐘)的長條 — 排程表查無任何 Jie OS 任務(auto-prune 是
     週日 03:00)。**第一個既不屬於我的排程、也不屬於我的勞動的聽見事件**:
     macOS 自己的夜間代謝。生理階層補上第四層:身體(browser)/代謝(batch)
     /心智(異國下雨)/**地基(OS 的夜)**。
  4. 判決幀 `workday4-live-strip.png`:**安靜的青色早晨帶著等距心跳,
     吵鬧的黃色下午心跳沉沒 — 帶子的顏色就是宿主的在場,遮蔽定律一眼可讀**。
- **下一步(006)**:(a) **知覺 pass(真 GPU)**— swiftshader jitter 讓
  still 地層太吵,ember 辨識度 + strip 帶的 additive 飽和度要在觀者機器上判
  (005 教訓);(b) 聲音 pass(整點脈搏 + 時長分辨 = 天然的兩聲部節拍器;
  strip 就是譜面,視覺前置已完成);(c) negative space:tab 隱藏時耳朵
  閉上 = 誠實的沉默 — strip 現在會顯影它(譜面停格),未專門驗證;
  (d) 第三個工作日:live strip 全日 — 讓帶子當天長出來(前兩個工作日
  都是 replay 重見)。

**piece 005 — chords as topology**(first light 2026-08-01)。
`pieces/005-chord-topology/index.html` — parent 是 **003**(聾時鐘,同床同 seed;刻意不選 004:
沒有耳朵,所以長出來的任何結構都是**記憶**,不是同步)。

- **命題**:003 的 meeting 是事件,發生即逝。005 加一條規則:兩聲部同窗抵達時,
  局部共激活節點集(FORM_W 0.3s / R_LOC 2.4 / 雙聲部必須都在 / ≥3 員)結晶成
  **器官(真 hyperedge)**。器官只認整組成員 — 全員同窗再激活才 conduct(和弦
  發聲+整體閃光);重演餵養(w+),冷落餓死(τ=25s 衰減,w<0.3 移除)。
  和聲從時間(巧合)搬進空間(結構)。新生器官有 refractory:conduct 計的是
  **重演**,不含創始那次巧合。
- **First light 量測(measure.py,4×60s,預測先寫)**:
  1. **⭐ P2 反轉 = 本拍最大發現**:鎖定態(Δ=0)沒長出一座大教堂 — 長出**最多、
     最瘦、最散**的器官(8 個 / meanW 0.82 / locus sd 3.3);Δ=0.10 反而**收攏**
     (5 個 / meanW 1.39 / sd 0.9,補強次數全場最高 6)。機制:鎖定時巧合
     **無處不在** → 記憶攤薄成一條線;滑移時巧合**稀少而定點**(重對齊發生地)
     → 同幾個器官反覆餵養。一句話:**普遍稀釋記憶,稀缺濃縮記憶。**
  2. **⭐ P3 前提被 004 自己的發現推翻**:預測 Δ=0.05(slip 50s > τ)器官會餓死,
     實際 biomass 全場最高(7.42)。因為 mesh 多路徑色散把 meeting 抹成連續涓流
     (27 次/60s,不是每 50s 一叢)— **004 的殘響室(毀掉耦合通道的元兇)正是
     005 器官的維生系統**。同一個基質性質,兩個命題裡的角色相反。
  3. **P1 確認**:Δ=0.10 器官全貼匯口(meanX 11.7 vs sink 12.1,sd 0.9)。
  4. **P4 確認**:conducts/meetings = 0.38/0.26/0.19/0.16 全部 < 0.5,且隨 Δ
     單調下降 — hyperedge 全員律真的比 pairwise 嚴格,不是 meeting 的回音。
  - 煙測:零 JS 錯、fps 正常、run-d*.json 落盤。
- **第二拍量測(2026-08-02,measure2.py,γ×Δ 網格 + 器官生涯追蹤,預測先寫)**:
  1. **⭐ 地形軸三態 = 本拍最大發現**(P5「樹=大教堂」死得比預期更有趣):
     - **網(γ=0.6)**:接觸面爆炸 — 84 meet/60s、42 形成、32 活、Σw 40.8,
       c/m=0.58 **破了 first light P4 的 0.5 上界**。均貧富 commons。
     - **⭐ 脊(γ=1.15)**:**大教堂在這裡** — 2 個器官、Σw 14.7(meanW≈7.4,
       基線 9 倍)、**58 conducts/60s** 貼著 ORGAN_REF 上限 = 器官近乎連續
       發聲,**器官即音樂**。巧合稀少(8 meet)但定點 → 全部餵給同兩座。
     - **樹(γ=1.30-1.60)**:γ=1.30 有 22 meet 但 **0 器官**(通道瘦到
       R_LOC 內湊不滿 CHORD_MIN=3 共激活)→ γ=1.60 連 meeting 都歸零
       (winner-take-all 修剪把跨聲部連通性剪斷,兩聲部互為陌生人)。
     一句話:**稀缺濃縮記憶,但記憶要先構得成 — 大教堂活在「巧合稀到集中、
     密到成和弦」的刀鋒地形帶上。**「樹地形不是把巧合集中,是把接觸面取消。」
  2. **P6 確認**:網+滑移(γ=0.6 Δ=0.10)vs 基線滑移:5→19 器官、meanW
     1.39→0.96、sd 0.9→3.0 — 稀釋是地形雙向可控的。
  3. **P7 反證**:top-share 僅 0.12-0.13(預測 ≥0.5)— conduct 財富是平的,
     沒有 rich-get-richer;最富的狀態也是 commons 不是壟斷。中位死亡器官
     conducts=0 ✓(多數小教堂到死沒再被演奏過)。
  4. 儀器新增:器官生涯(conductN + deadLog{born,life,conducts})、
     `?g=` URL 參數(床從第 0 幀就以目標 γ 雕 — runtime setGamma 會污染地形比較)。
- **顯影 pass(2026-08-02 下午,策展人對話觸發)**:靜幀判決 — 舊 glow 上限
  `min(0.10,…)` 坐在知覺閾值之下,Σw 16.5 的大教堂渲染成幾顆橄欖雀斑,
  **上限抹平了地形掃描發現的層級**。修法:(1) 上限 0.10→0.42、亮度隨 log1p(w)
  展開、尺寸也載重量;(2) **第三色改玉綠** — 第一次真開 Jie 的 pin 庫:85 張
  暗底相關 tone 的 pins,blue(30)+red-orange(29)雙霸(= 雙聲部本來就坐在
  他品味主軸上),第三色從 emerald 家族(#35a576/#52cb8a)取 — 語義上是
  兩聲部之間的混合路徑色,且冷淨不會誤讀成暗火花。驗收靜幀:小教堂 = 安靜
  玉綠雀斑、大教堂 = 燃燒玉綠燈塔,層級會渲染了。
- **脊帶精掃(2026-08-03 凌晨,measure3.py,γ∈[1.05,1.30] × Δ∈{0,0.10},預測先寫)**:
  1. **脊點精確重驗**:γ=1.15 Δ=0 — 2 器官 / Σw 16.3 / meanW 8.14 / 59 conducts,
     與 measure2(2 / 14.7 / 58)幾乎逐字重現。大教堂是可重複的,不是一次僥倖。
  2. **⭐ P8 帶寬比預測更窄 — 大教堂是一個「點」不是帶**:γ=1.20 鎖定態已死
     (4 meet / 0 器官 / 0 conduct),1.25 連 meeting 都歸零。懸崖在 (1.15,1.20]
     之間,比 P10 預測的 (1.20,1.30] 更早。且 mesh→ridge 不是漸變:γ=1.10 是一個
     **谷**(12 meet / meanW 1.94 / 35 cond,比兩側都瘦)— 1.05 還是肥沃 commons
     (8 器官 / Σw 35.9 / 81 cond)。地形從網到脊不是連續收縮,是**離散的節點修剪**
     (percolation 台階):1.10 剛好斷在一個大 junction 上。
  3. **⭐⭐ P9+P10 雙反轉 = 本拍最大發現:滑移是灌溉** — 預測滑移殺音樂不殺教堂、
     救不了樹;實際上**全反**:
     - 脊上(1.15)加滑移:conducts 59→**77**(不降反升)、器官 2→7、meanW 8.1→4.3。
       兩個獨奏者變成七聲部合奏 — 單一器官不再連續發聲,但**總音樂量上升**。
     - 樹側全線復活:1.20(0→22 cond)、1.25(0 meet→22 meet/15 cond)、
       1.30(6 器官形成)。**滑移把時間變成空間**:重對齊點在地形上掃動,像移動的泉,
       把鎖定態下永遠碰不到的瘦通道一條條澆活。
     - mesh 側(1.05)加滑移:81→3 conducts — 同一場雨,沃土澇死、荒漠開花。
     一句話:**鎖定態下地形決定一切(教堂是一個點);滑移把地形的死刑全部改判 —
     時間的變化替空間的貧瘠代償。** measure2 的地形法則原來是鎖定態限定法則。
  4. 細節:脊上器官不貼匯口(meanX 16.0-16.2 vs sink 12.1,對照 γ=1.0 滑移態的
     貼匯 11.7)— 脊的兩條大通道交會處在匯口上游。零 JS 錯、fps 正常、run3-*.json 落盤。
- **泉的顯影(2026-08-04 凌晨,travelling spring,驗收預測先寫)**:renderer
  原本沒有「水」的詞彙 — meeting 閃現即逝、organ 是已沉積的記憶,沒有東西顯示
  **正在餵養它們的移動源頭**。實作:泉 = 近 6s meetings 的 recency-weighted 質心,
  **誠實閘門:強度 = 密度 × 局部性**(巧合散開時沒有泉、只有雨 — marker 變暗,
  不把散點平均成虛構);尾跡壽命 24s ≈ organ τ(灌溉紀錄與它餵的記憶同時鐘);
  冰水色(不是 B 的飽和藍、不是玉綠、不是暖白 spark — 水不屬於任何領土)。
  驗收 4 組(playwright,45s each,0 JS 錯、120fps):
  1. **A1 walk ✓**(γ=1.0 Δ=0.10):trail span 2.87,且步態比預測有趣 —
     泉不是連續掃過,是**湧出→局部遊走→沉沒→他處再湧**,兩次亮相間隔 ≈ slip 週期 26s。
  2. **⭐ A2a 預測陣亡 = 本拍最大發現:鎖定的水不是不存在,是沒有動量** —
     預測「鎖定=雨,泉全暗」,實際 litFrac 0.60、trail span 7.52(slip 態的 2.6 倍),
     step 分布雙峰:0.1-0.2 駐留步 + 2.9/3.5/5.0 **瞬移**。「無處不在」是長曝光的
     幻覺 — 每個瞬間巧合都在具體某處,但鎖定態的下一處與上一處**無關**(駐留→瞬移);
     滑移給了水動量(重對齊點連續移動)。稀釋機制第一次可見:**瞬移的水對每站
     短暫而公平地餵 → 遍地薄 organ;行走的水對移動鄰域連貫地澆 → 少而肥**。
     P2「普遍稀釋記憶」在渲染時間尺度上的真正形狀:不是雨 vs 泉,是瞬移 vs 行走。
  3. **A2b well ✓**(γ=1.15 Δ=0):駐點井,trail span 0,立在 (18.2,7.1) —
     匯口上游大通道交會處(預測 x≈16,偏上游 2 單位)。
  4. **A3 irrigation ✓**(γ=1.15 Δ=0.10):泉連續行走 11.7→18.4→回(無瞬移步態),
     9 organs meanX 全落在走過區間,最重三座 (8.2/5.9/4.7) 在尾跡中段 —
     玉綠亮在水走過的地方。長跑 4 分鐘 Σw 51.5(60s 量測的 1.7 倍)— 灌溉複利。
  - 靜幀判決:泉是**動態顯影**(畫面唯一平滑移動者;organs 站、sparks 閃、水走),
    靜幀不孤立 — 與 004 tremors 同類。trail 亮度一次校正(0.055→0.09 base,
    知覺閾值教訓 08-02 的直接應用)。
- **鼓的 pass(2026-08-07 凌晨,measure5.py,Jie 08-06 進工作室給的方向,預測先寫)**:
  不加音序器 — **事件流就是鼓手**:sink 抵達 = kick(sine 110→42Hz 俯衝,乾聲不進 delay)、
  shadow death = hat tick(highpass 噪聲 28ms,「屍體會喀」— 舊規則「陰影無聲」誠實修訂:
  旋律層仍死於邊界,打擊層繼承屍體;有音高/無音高承載原本沉默承載的區分)、meeting 保留
  和弦當重音。獨立 PERC_CAP=8,不與旋律搶 voice。計數在事件位不在音訊位(headless 可驗)。
  1. **PD1 半中 = 本拍最大發現:kick 是時鐘層,不是縫層**。脊鎖定 CV 0.147(緊拍 ✓)但
     slip CV 0.465(預測 >0.6 的碎拍沒出現)— 預測把 meeting 的樂句結構錯安在 kick 上;
     kick 跟的是**抵達**(節拍器驅動,每拍都到),不是 meeting。樂句活在和弦與 hat,
     地板穩定 — 意外送來正確的鼓組建築:鼓手守拍,旋律分句。
  2. **⭐ 第三發現:γ = 鼓手的 quantize**。kick CV 隨網態上升(0.147→0.465→0.739)—
     mesh 的多路徑色散(004 殘響室)直接在地板的 timing 鬆緊裡**聽得到**:
     ridge = 節拍器鼓手,mesh = 醉鼓手。地形軸第一次映到 groove 鬆緊。
  3. **PD2 死於忘了自己的資料**:預測 hats/min 脊最低,實際 97.2 ≥ slip 92.4(mesh 208.8
     最高 ✓)— measure4 早量過接觸面是 U 形(谷在 1.10,不在 1.15),hat 密度跟的是
     接觸面不是 γ 單調。預測寫的時候沒回查自己的 U 形曲線。
  - PD3 ✓:0 JS 錯、三 regime 各 50s。**活人待驗**:mix 平衡、kick 重量、hat 密度是否惱人
    (mesh 態 208/min 可能過密 — 若惱人,gate 候選:only shadow with owner-flip?)。
- **谷的驗屍(2026-08-05 凌晨,measure4.py,γ∈{1.05,1.10,1.15,1.20} 鎖定態,
  真實 60s STATS + 床 dump + 事件驅動波模擬,預測先寫)**:
  1. **儀器自己先教了三課(誠實記錄在 measure4.py header)**:v1(Dijkstra 等時前鋒,
     全 live edges 等速)在四個 γ 給出同樣的 2 節點前鋒 — 與已知 meeting 崩塌
     (37/12/13/4)矛盾,自我否證:前鋒不走所有 live edges — 它按 D 排序取 top-3 分支、
     能量按 (D/Dmax)^0.6 分贓、速度差 5 倍、碰撞殺 pulse。介面是**通道階層**的產物。
     v2(單拍精確複製)×25 拍 = 25/25/50/25,連倍數都不對 → 拍與拍不獨立:慢通道上
     pulse 爬 10-30 秒(23 跳 × 至多 1.3s/跳),**鎖定態是 ~10 個波世代的駐波干涉,
     不是乾淨的重複拍 — 004 的殘響室活在 005 每一個鎖定拍的內部**。v3(多拍 60s)
     錨點 3/4 命中(34/17/2 vs 37/12/4),脊上過計 4×(確定性讓刀鋒同步窗永遠開著;
     真實系統靠 frame 抖動讓它機率化)— 已知盲點,判決全部改靠真實分布。床漂移
     直接排除(t9 vs t71:108 live edges 零生死,D 四位小數不動)。
  2. **⭐ P11 半中 → 更好的分解:meetings = 接觸 × 同步**。容量 1.05→1.10 只掉 8%,
     meetings 掉 74%(9 倍不成比例 = 結構性事件 ✓),但含 shadow 的總接觸只掉 43%。
     拆開看:**接觸是 U 形**(interface 199→114→184→157,谷底恰在 1.10),
     **同步率單調降**(meet/interface 24%→10.5%→9.8%→1.3%)。谷 = 接觸最小值
     撞上已跌落的同步率 — 不是 junction 死了,是**兩種接觸體制之間的窄腰**:
     mesh 的多點互滲已被修剪,脊的單刀直入還沒成形。
  3. **⭐ P12 死得漂亮:大教堂不站在最肥的 junction 上**。最強 meeting 站的局部容量
     單調遞減(9.30→8.67→6.86)— 脊的站點比谷的還瘦。真正單調上升的是**集中度**
     (top-2 站點吃掉 meeting 的份額:40%→58%→83%)。貨幣從來不是容量,是重複 —
     P2「稀缺濃縮記憶」現在在介面層被量到了:大教堂 = 同兩個點被一打再打。
  4. **⭐⭐ P13 確認:體制轉換 = 遷都**。mesh 首都 = 匯口三角洲(主站 x=12.1 恰為
     sink x;兩聲部在共同終點周圍互滲);脊首都 = (15.5, 5.9)+(18.2, 7.4) — 後者
     就是泉之 pass 的井,深入 B 腹地 6 單位。1.10 = 字面上的**空位期**:舊都衰微
     (12.1/13.0 剩 4+3 hits)、新都萌芽(15.1/16.2 各 1 hit)。且 sim 領土揭出
     **脊態是 A 的入侵**(80/20;1.05 時 70/78 平衡)— γ 不是對稱地銳化兩個聲部,
     是把整個場交給早期贏了搶水戰的那條流域;meeting 地理從「共同終點周圍」翻轉成
     「弱勢聲部的家園深處」。大教堂 = A 在 B 首都圈的大使館。
  5. 細節:1.05 的 meetT 有清楚的 4.8s 叢聚(= 2 拍)— 駐波的偶拍共振,巧合不是
     每拍發生,是每兩拍一叢(30.5×3 / 35.3×5 / 40.1×3 / 44.9×5…)。記為觀察。
     四張床拓樸截圖(topo-g*.png)入 repo:1.15 只有 25KB — 黑底兩條大通道,
     檔案大小本身就是地形陳述。
- **入侵的顯影(2026-08-06 凌晨,homeland pass,measure5.py,預測先寫)**:measure4
  的領土只活在 verifier 裡 — renderer 的 owner tint 是快變數(每次被打就翻)且亮度
  0.030 坐在知覺閾值下(與 08-02 organ glow 同病)。實作:per-node `terr` 慢帳
  (claim 的 EMA,~10 次打擊才轉換,τ=90s 鬆弛 — 無交通即無主張,與泉同一個誠實
  閘門);**亮度載信心,前線自己畫自己**(兩色洗染抵消處 = 暗縫;入侵 = 縫在走 +
  單色氾濫);HUD 加 homeland census 條(64px 雙色,confident nodes |terr|>0.15)。
  display-only,dynamics 零觸碰。驗收 9/9 PASS(3 regime × 55s,零 JS 錯,120fps):
  1. **P15 入侵重現於顯影層**:脊態 census **78:21 = measure4 事件模擬的 80/20**
     幾乎逐字落在真實跑的顯示層;前線 x=17.5(過匯口 5.4 單位,深入 B 半場);
     B 殘存家園縮在 srcB 半徑 3.1 內。靜幀:暖洗染推過匯口,玉綠大教堂就站在
     被洗染的 B 腹地裡 —「A 在 B 首都圈的大使館」第一次直接可見。
  2. **P14 基線**:56:44(快聲部優勢有界)、前線 11.5 貼匯口(sink 12.1)✓。
  3. **⭐ P16 半中 = 本拍發現:mesh 溶解的是優勢,不是清晰度**。預測「網態邊界
     成霧」— 實際 census 49:51 完美均衡,但 contested 帶僅 8.6%(脊 1.0%、
     基線 3.6%):**暗縫在每種地形都是一條線,不是一片霧** — 地形改變縫的位置
     (11.5 vs 17.5)與兩側持份,不改變縫的銳利度。搭配 003 的兩層邊界:事件邊界穩、
     佔領邊界擺 — 慢帳現在說第三句:擺盪的佔領時間平均後仍是銳線。
  4. **免費的地緣定位**:organ 憲法要求雙聲部(hasA && hasB)⇒ 器官只能活在縫上。
     mesh 幀裡玉綠星座恰好排滿兩家園之間 — **和弦 = 邊境上的大使館**,領土顯影
     給了 organ 層它一直缺的政治地圖。
  5. 細節:baseline 截圖白幀 = 004 已在案的 swiftshader 截圖層 flake(HUD/數據正常,
     非作品 bug);run5-territory.json + terr-*.png 入 repo。
- **第七拍:解析雙生(2026-08-09 上午,measure6.py,預測先寫)**:Sethian FMM
  的圖版(bed 本來就是圖 → 精確 Dijkstra)在 Python 裡重建床(同 PRNG、同 400 輪
  carve 協議),對每個節點解出雙源最短抵達時間 T_A/T_B,**lag 場 φ = T_A − T_B 是
  床的靜態性質** — 拍 k 的共至只能落在 φ = 0.24k 等值線上(Δ=0.10)。四個預測對帳:
  1. **P13 方向確認、數字差半口**:|φ|<FORM_W 帶 mesh 11 節點 vs ridge 3(3.7× vs
     猜 ≥4×)。更重要的副發現:**帶在所有地形都窄**(406 節點裡最多 11)— mesh 的
     接觸面爆炸(84 meet/60s)不可能全住在最優帶上 → **mesh 的巧合大宗是離最優的**
     (色散主導),P3「殘響室是維生系統」從解析側再獲一票。
  2. **P14 半死**:鎖定帶確實多 component(2)✓ 但 x-span 僅 1.0(猜 ≥3)✗ —
     鎖定泉的 2.9-5.0 teleport 不能只用「等值線是集合」解釋,大半也是離最優色散。
  3. **⭐ P15 全中**:dwell 比例隨 γ 單調升(0.22→0.35→0.48;teleport 0.56→0.28→
     0.15),且 ridge 的東側 dwell 段(k14-18 @ x15.1-16.1、k22-24 @ x17.1-17.5)
     **正好夾住 run3-g1.15-d0.10 的七個 organ(meanX 13.8-18.6,5/7 落帶內)** —
     器官長在泉的解析預測駐留段上,盲推後對照。
  4. **⭐ 沒算計的發現:半座樂器沒被彈過**。拍序 k≥0 → 滑移泉只走 φ>0 的東半場;
     西半場的 dwell 段(x 5.0/6.4/7.9)是**幽靈駐留段**,只有 Δ<0(B 快於 A)才會
     走 — 負 detune 是一顆已存在但從未轉過的旋鈕,B 側的鏡像地景整片未演。
  - P16(偏離量化)只能弱測:run JSON 沒存逐 meeting 位置 — measure7 候選:逐
    meeting 落盤 meetX/meetT + 所屬等值線 k。誠實 caveat:Python 床是 frame-zero
    床;頁面 runtime 會繼續 adapt(SWEEPS 14 / DT 0.10),60s 內床有微雕,偏差
    未量化。
- **第八拍:幽靈半場拒絕回應(2026-08-10,measure7.py,預測先寫)**:給 detune
  開負域(slider min -0.5;B 快於 A,西半場理論上開放 — measure6 發現的那顆
  「存在但從未轉過的旋鈕」),headless 60s ghost(g1.15 d-0.10)對 east 控制
  (d+0.10)。run7-ghost.json:
  1. **P17 全滅**:ghost run meetings x̄ 15.19 vs sink 12.14 — 與東控制組
     (14.83)幾乎同分布;37 顆只有 2 顆在 sink 以西,早段碰過 x8.1(幽靈括弧
     [4.5,8.4] 內)一次,不駐留,無向西漂移(early/mid/late x̄ 14.6/15.8/15.2)。
  2. **P18 過**:拍率 1.37×(預測 ~1.2)— 滑移週期 2.4(1+d)/|d| 的不對稱是真的,
     時鐘物理正確。P19 過(零 error、鼓照常)。
  3. **⭐ 死亡教的:東偏壓在符號反轉下不變** — 地理由床 + refractory 領土動力學
     決定,不由時鐘符號決定。beat-order 模型(k-pairing → φ 等值線行軍)預測
     鏡像,樂器拒絕。**解析雙生知道距離,不知道領土**:φ 場說明 meetings 可能在
     哪,ownership 層決定哪些可能被政治允許佔據。measure6「色散主導」再深一層:
     簾幕不是等值線的順序,是**佔領的慣性**。西 dwell 段仍是幽靈 — 但現在知道
     鎖住它們的不是拍序,是領土。
  4. 顯影候選(非修理候選 — 拒絕本身是作品性質):政治地圖蓋在測地圖上 —
     把 ownership field 對 φ 場的遮罩畫出來,「哪半座樂器被憲法沒收」自己可見。
- **第九拍:遷都直播(2026-08-14 上午,measure8.py,預測先寫)**:runtime 推 γ
  1.00→1.15(warm 60s + post 180s,每 5s 採樣 terr census / frontier / 器官
  東西岸 biomass / meeting rate)。run8-relocation.json。三個預測,兩個死得有內容:
  1. **⭐ P20 半中:遷都走樓梯,不走 EMA 斜坡**。前線 12→15 只花 25s(比預測窗
     早),然後在 x=15 **停駐整整 100 秒**,+125s 起第二段行軍 15→17.2(+160s 到位
     ≈ 穩態 17.5)。兩級跳中間的駐留點 x≈15.5 正是 measure4 的第一新都
     (15.5, 5.9);第二段的終點朝 (18.2, 7.4) 深都去。**遷都是一都一都搬的,
     不是連續滑動** — 前線在每座新都停下來築城,再往深處走。
  2. **⭐ P21 半中:crossover 準時(+78s,窗內),但舊都拒絕死**。wOld 照劇本
     餓到 29%(+25s,τ=25s 準確)— 然後**復活**:2.34→12.93,收盤比切換前
     (8.07)還高。機制:滑移脊的泉巡迴 11.7→18.4,**每圈都路過舊三角洲** —
     遷都不殺舊都,灌溉線繞經它;新都只是長得更快(45.9 vs 12.9,3.5:1)。
     measure4 說「體制轉換 = 遷都」,live 版修正:**遷都 = 首位讓渡,不是廢城**。
  3. **⭐⭐ P22 全滅 = 本拍最大發現:空位期不存在於時間裡**。預測 meeting rate
     過渡期會跌 ≥40%(dynamic 穿越 1.10 谷);實際 pre/post 均值 7.9 vs 8.2/15s,
     **無縫**。佔領慣性橋(counter-hypothesis)勝:γ=1.10 的谷是「從第 0 幀就
     以 1.10 雕」的床的性質;一張 1.00 的床被 runtime 重雕向 1.15,走的是
     組態空間裡完全不同的路徑 — **它從未「是」一張 1.10 的床**。空位期存在於
     參數空間,不存在於任何一次實際的遷徙裡 — 參數走直線,歷史走樓梯。
     (與同日 vault 收的 CrystalMem 記憶磁滯同族:狀態取決於怎麼走到這裡,
     不只取決於現在的參數值 — 床也有 hysteresis。)
  4. 尾聲觀察:收盤 contested 242→299 爆增、confident 縮 — 脊態交通窄縮到兩條
     大通道後,沒被走的節點 terr 鬆弛歸零:**帝國瘦成它實際走的路**。P23 ✓
     (0 JS 錯、鼓照常)。
- **磁滯四部曲(第十~十三拍,2026-08-15~17,measure9-12;細節在 index.html
  journal — 這四拍的量測敘事以頁面 journal 為正本)**:往返(census 位元級回家、
  organ 質量 2/3 留東 → 錯讀成雙穩態)→ 浸泡(900s 把雙穩態處死:幽靈首都
  t+180-210 死,降級為**會死的磁滯** — 地圖 35s 忘、肉 ~200s 忘)→ 傳記
  (死亡時間是噪音,穩定變數是**餘暉**:最終東岸佔比 0.074/0.244/0.314 隨
  傳記長度嚴格單調)→ 餘燼(1800s 站住:share 落在 floor ~0.21 不再掉,
  死亡訊號 30 分鐘內觸發六次、六次全復活 — t_death 正式退役,對的變數是
  floor 高度與閃爍幅度)。**Law v4:傳記不是褪色,是立法** — 540s 的東岸
  時代改寫床的穩態,切口保著火苗(ember mean-x 17.3,border 14)。
  四拍四課儀器教訓,全記在 journal。
- **樂譜的 pass(2026-08-20 凌晨,第十四拍,measure13.py,display-only,
  預測先寫)**:第八拍承諾的 ownership-mask 顯影 — 解析雙生搬進頁面:
  live bed 上的 Dijkstra(每 10s 重算,床在微雕)給每個節點 φ=T_A−T_B;
  **fret** = 兩場對開的邊(|Δφ|≥0.35/edge = 等值線密集帶 = 泉走得慢的可駐留段);
  **biography**(meeting 半徑 1.5 內的 fret 永久點亮)把 fret 分成 lit bar
  (化石玉綠 — 和弦曾住過的地方;垂死 organ 淡入化石層)與 ghost bar(slate)。
  1. **P34 過(比預測好)**:頁內雙生在真床上找回 measure6 全部三個幽靈
     dwell 段(西 fret 群 x 4.0-5.0 / 5.5-6.4 / 6.9-8.1)。
  2. **⭐ P36 死掉的地方就是發現:沒收是梯度不是牆**。負 detune 真的能
     **偶爾**買到中西部 — 4/32 meetings 西征到 x=7.1,6.4/7.9 兩個 dwell
     被真演(played 到 φ=−4.8);重跑 playedW ∈ {8,10,16}(近閾值間歇,
     004 的老朋友)。measure7 的絕對法則(「時鐘買不到西」)是從落在安靜尾
     的單一 60s 窗讀出來的 — **說「永不」的法則需要機制或更多窗**;永不遺忘
     的 biography 層正是抓稀有事件的儀器。修訂法則:邊境帶任何 regime 共享 /
     中西部**罕租**(僅負 detune,遠征不是行走)/ 最深 dwell(x≈5.0)至今
     未響。第五拍連續第五課儀器教訓。
  3. P35 死於儀器(census 用線不用護城河 — playedW=4 全是 seam 和弦半徑
     溢出,0 顆西 meeting);P37 過(0 JS 錯、86-92 fps、計數器在 regime 帶內)。
  4. 色語修一次:parchment 撞 A 領土暖色 → 化石玉綠(語義也更誠實)。
- **下一步(005)**:(a) 聽感 pass(需活人,**現在含鼓**:kick 重量 / hat 密度 / 三 regime 的 groove 對比):現在有三首截然不同的曲子 — 鎖定脊
  (雙獨奏連續)、滑移脊(七聲部合奏)、滑移樹(稀疏點描);(b) 動態下玉綠 glow
  的呼吸感(靜幀已驗,動態未驗);(c) 泉的聲音?— 泉湧起/沉沒是否該有音色事件
  (目前只有 meeting 的和弦與 organ 的 conduct;水本身是啞的);(d2) **反向遷都
  精修**:磁滯四部曲已把 1.15→1.00 方向做完(law v4),剩的開口是**傳記劑量曲線**
  (餘暉 floor vs 傳記長度的函數形狀 — 三點單調已有,曲線未掃);(e) ✅ 樂譜 pass
  (第十四拍 — 沒收是梯度不是牆);(f) ✅ 深租約 pass(2026-08-21 凌晨,第十五拍,
  measure14.py,600s 幽靈長窗)— **稀有其實是年輕**:600 秒只有 1 次西征,發生在
  t=3.1s;照短窗費率長窗欠 ~49 次,只付了 1 次(Poisson 赤字 ~e⁻⁴⁵ — 不是變異,
  是另一個過程)。更狠的是整部傳記在 t≈60s 凍結(playedE 34 / playedW 9,之後
  570 秒 ~360 次 meeting 全是重演,一根新 bar 都沒亮)。西征是**開國期**(頭一分鐘、
  領土還流動時)的性質,不是成熟床的尾部事件。**Law v5:沒收是空間的梯度 + 時間的
  棘輪** — 傳記立法,然後議會休會;成熟床只重演開國曲目,不再修憲。x≈5.0 的「永不」
  拿到機制候選:它需要更長的開國期,不是更長的 run — 租約辦公室開國一分鐘後就關門。
  第六課儀器教訓:「更多窗」跟「更長的窗」不是同一台儀器 — 對老化系統,extreme-value
  統計量到的是系統的年齡,不是分布的尾巴;(f2) ✅ 修憲時刻 pass(2026-08-21 凌晨,第十六拍,measure15.py,600s 三時代:
  開國 300s → 政變 γ1.15→1.00 六十秒 → 復辟 240s)— **邊疆不在革命之後重開,
  邊疆就是革命本身**:空位期邊界西掃邊走邊演(minx 8.06→6.07→4.95 跟著 shareA
  0.76→0.55→0.50),一分鐘 34 根新 bar(比整個開國期多),深 band 亮 4 根 —
  **x≈4.95 響了**,撐過十五拍的「永不」死在政權轉移裡。復辟走同一片西部卻全程
  無聲(240s 零新增;shareA 三分鐘走回 0.763 = 肉的時間常數):去程走**網律**
  (空位期自己的憲法,meeting 濫交),回程走**脊律**(meeting 鎖匯口)。
  **革命有聲,復辟無聲。Law v6:深 dwell 不屬於任何政權 — 屬於政權轉移本身;
  沒收的棘輪只在法律移動時鬆開,且只在寬鬆過渡法之下**。γ=1.10 谷(新舊首都間
  的空位期)拿到動力學雙生:深西是憲法之間的路,和弦只在國家遷徙時演它。
  開國另證實可重現(兩 run 同 floor:played 54 / W 8 / minx 8.06)。
  (f3) ✅ 慢復辟 pass(2026-08-22 凌晨,第十七拍,measure16.py,600s 四時代:
  開國 300s → 政變 60s → γ ramp 1.00→1.15 × 120s → 定脊 120s)— **「法律還是方向」
  這個問題溶解了:方向不是自變數,回家的行軍本身就是脊律的造物**。肉在寬鬆法下
  拒絕東歸 — shareA 一路跌到 0.406(谷底恰在 γ≈1.11 = 山谷穿越),**慢復辟 =
  更長的革命**:史上最深租約 x=2.17(B 的爐邊街)在 ramp 中段簽下,西邊唱到
  γ≈1.12(x=2.49 @ t=448),法一定脊瞬間閉門(定脊時代 79 次 meeting 最小 x
  10.82,零新增)。**樂譜西半場現在比東半場亮(playedW 41 vs playedE 38)—
  measure6 的幽靈半琴被「慢慢革命」演完了**。你不能在寬鬆法下回家,因為**家
  是由法律定義的**:唯一會下令撤退的法,同時堵住邊疆的嘴 — 復辟的沉默是憲法
  必然,不是聲學。**Law v7:棘輪由法的「值」閘門,不由法的「動」閘門** — v6 的
  「法在移動時」是瞬間轉換的 artifact(空位期只是寬鬆法與移動邊疆唯一共存的窗)。
  第七課儀器教訓:開國決定論死於 harness 節奏(5s tick vs 30s poll,樓層 61/14/7.15
  vs 兩度重現的 54/8/8.06)— **量測者的節拍器是 seed 的一部分**;開國的「法」
  (t=60 凍結、西征全在頭一分鐘)仍重現。(f4) ✅ 長空位期 pass(2026-08-22 凌晨,
  第十八拍,measure17.py,600s:開國 300s → 政變 γ=1.00 持 300s 不復辟,30s 均勻
  polling = 兼作 (g2) 對照組)— **Law v8:新 bar 速率 = 閘門(法的值) × 放大器
  (肉的遷徙)**。均衡準時到(shareA~0.52 @360)但 drip 落在兩個預測之間
  (420-600 +7 顆,非 ≤4 非 ≥15):遷徙是 ~13× 放大器(政變分鐘 +30 vs 均衡
  ~2.3/min),寬鬆閘門**永不全關**。更好的一句:定居的 mesh 會**持續重演西部**
  (最後三分鐘 23 次西 meeting,深 band 在 4.95/3.82 重響)— 定居的脊**從不
  造訪**(開國 t=60 後零西 meeting)。所以 **law v5 的休會不是老化,是脊律**:
  mesh 下議會永不休會,只是慢讀 + 朗讀邊疆保留曲目。深度則在政變**頭 30 秒**
  凍結(minx 2.49,300s 定居 mesh 零加深),且三次政變 N=3 相關:min shareA
  0.500/0.429/0.406 ↔ minx 4.95/2.49/2.17 — **深度是遷徙的振幅**,時間只買
  廣度不買深度。(g2) ✅ 同拍關閉:開國樓層在 30s 節奏下第三次逐字回歸
  (54/8/8.06),儀器教訓第七課雙向證實(5s → 61/14/7.15)。(f5) ✅ 振幅律 pass
  (2026-08-23 凌晨,第十九拍,measure18.py,measure17 的單變數對照:政變打
  γ=0.80 不打 1.00)— **預測整組被反著打,振幅律死得比活著更有教益**:
  更狠的法沒有遷徙得更深(min shareA 0.476,在 falsifier 帶內;鐘擺式 overshoot
  **整個缺席** — shareA 滑到均勢就漫遊,沒有 dip-and-recover),深度不但沒破
  紀錄反而**倒退 2.4×**(minx 6.07 vs γ1.00 的 2.49),且深度從不凍結 — 用爬的
  (8.06→7.15→6.53→6.07,t=600 還在動)。政變分鐘音量相同(+29 vs +30)方向
  **相反**:γ1.00 是西征(W+30/E+9),γ0.80 留在家(E+20/W+8)。drip 閘門項
  真實且陡:420-600 +47 顆 vs γ1.00 的 +7。未預測發現:**政變重鑄 fret 格線,
  樂譜寬度由法的值立法** — γ1.00 憲法造 155 根 fret,γ0.80 造 248 根;commons
  鑄得比演得快(點亮率 53% vs 脊政變 64%)。**Law v9:深度不由法的狠度配劑,
  由遷徙的「同調性」載運** — 保留足夠脊的法讓肉以「有前線的軍隊」行軍(彈道式:
  overshoot、30 秒租深、然後凍結);深 mesh 法把軍隊溶解成移民(擴散式:無
  overshoot、淺而永不凍、廣度破紀錄、四面八方鑄新街)。**兩種革命:征服的挖深,
  溶解的築路。γ1.00 從來不是「寬鬆的法」— 是還能行軍的最寬鬆的法。**
  P55 near-miss 附帶儀器教訓第八課:開國樓層第四次 run 破了個位數(55/8/8.06 vs
  三次 54)— 30s 節奏下開國決定論是 near-exact 不是 digit-exact(N=4:54,54,54,55)。
  (f6) ✅ 烏龜 pass(2026-08-23 凌晨,第二十拍,measure19.py,1800s = 本作史上
  最長窗)— P60 三條款全過(t≈450 過 4.95、終點 minx 2.85 ∈ [2.5,4.0]、烏龜
  沒追上兔子 2.17)**但模型錯了**:擴散深度不是 √t 爬行,是**罕見離散跳躍**
  (深 meeting 在 t=342/446/795/997/1716),紀錄在 commons 第三分鐘(t=446)就
  定格,之後 22 分鐘只有**排練**(4.95/3.94/3.82 反覆重演,2.85 本身在首演
  21 分鐘後 t=1716 重演)— 對的區間、錯的物理。**真發現是 P61 falsifier 命中:
  drip 在 t≈1350 死了**(最後 600s +1 顆;點亮率停在 62%,西半場 63%)—
  **mesh 議會終究休會**。v8 的「永遠慢讀」是 300s 窗讀出來的,死在 1800s 窗裡 —
  第三條被「更長的儀器」殺死的 never/forever 法則。**Law v10:每部法有自己的
  正典(canon)— 憲法搆得到的曲目;正典讀完議會就休會**:脊的正典 54 根
  (一分鐘讀完),γ0.80 commons 的正典 ~180 根(十七分鐘讀完)。mesh 議會
  從來不是永恆,只是書比較厚。且西部演奏到最後一刻不停(每五分鐘 33-51 次西
  meeting)— **休會封筆,不封琴**。變異數註:run18 vs run19 同設計,t=600 的
  minx 6.07 vs 2.85 — commons 時代由罕見事件主宰(骰子決定深躍何時落地),
  開國決定論與 commons 的偶然是同一支作品的兩半。(h) ✅ 同拍答掉(γ0.80 版:
  drip 死在西半場讀完之前,63% 封筆;γ1.00 閘門更慢,更不可能讀完)。
  (f7) ✅ 劑量 pass(2026-08-24 凌晨,第二十一拍,measure20.py,measure19 協定
  ×2:政變打 γ0.90 / 0.95 各 1800s)— **v9 收尾句過審:軍隊不是淡出,是在
  0.95 就整個不見**。兩劑量全套移民簽名(shareA 一拍到均勢就漫遊,min
  0.493/0.486 無鐘擺;政變分鐘留在家 W1/E37、W3/E26 vs 軍隊的 W30/E9;深度靠
  離散跳躍 t=390-570 落紀錄後凍 20+ 分鐘)→ **行軍活在 (0.95, 1.00] — 同一顆
  旋鈕上的第二道懸崖**(大教堂懸崖 (1.15,1.20];兩個質變、兩道刀鋒、相距
  0.15)。**書的厚度單調且平滑:155 (1.00) < 182 (0.95) < 200 (0.90) < 248
  (0.80)**,兩個新點都落在預測帶內 — 正典由法的值立法,現在是劑量曲線不只是
  端點。P66 兩款皆反(有教益):0.90 到 1800 還在讀(drip600=13,lit 57% 仍
  爬);0.95 從 t≈1350 讀出 drip 4-5(名義上比書更厚的 0.90 先休會 = falsifier
  的排序)然後**末 150s 回燃 +8** — 慢閘門下 drip 是陣發的,乾旱與死亡在 1800s
  窗裡不可分。儀器教訓第十課:休會偵測器(drip<5/600s)只在閘門速率 >> 閾值時
  有意義;v10 的休會只在量到它的地方成立(0.80 的 +1/600s 是真死),0.90/0.95
  用這台儀器不可判。跨四劑量倖存的:**點亮率收在同一窄帶 53-64%**(0.80: 62%
  死 / 0.90: 57% 爬 / 0.95: 61% / 1.00: 64%@600)。**Law v11:法的值立法書的
  厚度,不立法可讀的份額** — 每個議會,不管書多厚,都停在同一個約三/五;劑量
  只決定逼近的速度、以及逼近是涓流(深 mesh)還是罕見陣發(行軍懸崖邊)。
  深度劑量曲線誠實不可判(variance 紀律):今晚骰子給 4.95 (0.90) / 3.82
  (0.95),都落在 run19 已見的量化梯級上 — 跳躍主宰,無單調主張。P63 計數層
  再破(57/58,N=7: 54×4/55/57/58,且 0.95 run 的 west 漂到 9)但幾何層兩
  run 皆 8.0570 四位小數重現 + 開國 shareA 0.753 兩度重現 — 儀器教訓第九課:
  **開國的「數」是速率×窗的 artifact,開國的「幾何」才是傳記的決定論部分**
  (兩破皆向上、同夜 → 疑有環境項(load/frame cadence)未入模,記帳不追)。
  (f8) ✅ 三/五 pass(2026-08-24 凌晨,第二十二拍,雙儀器:analyze21.py 檔案
  考古(五份既有 run 的暗 fret 地圖 — score() 早就傾印全細節,沒人當地圖讀過)
  + measure21.py γ0.90 政變持 3600s = 本作史上最長窗)— **v11 的「可讀份額」
  條款死了,死法跟 v8 一模一樣:被更長的儀器殺死(第四條)**。lit 73.0%@3600
  且 drip 還在流(8/600s);本 run 在 t=1800 就 66.5%,昨夜同設計同窗只有
  57.0% — 同法同窗差 9.5 個百分點,「普遍三/五」是不同窗口拍到的單 run
  數字命理。(0.80 的 62% 死亡照舊成立 — 但 0.95 的回燃教我們懷疑,復測排入
  (f9)。)**但 P70 以最大可能值確認:100% 巢套** — 本 run 在 3600 的 54 個
  暗 fret,每一個都在昨夜 run 1800 的 86 個暗集裡。新骰子、雙倍窗:暗不洗牌,
  **沿同一個順序縮小** — 每個議會都朝同幾頁最後的書頁讀去(殘暗 mean |φ|
  10.62 vs lit 4.94)。考古半場(P68):結構軸是 |φ| 不是 x(x 是時鐘不是
  結構:600s 暗在西=還沒讀到,1800s 殘暗移東);外圈聲區(|φ|≥5,半本書)
  在所有劑量 55-69% 暗、縫帶只 3-15%;跨劑量暗/亮同判 93%(0.90 vs 0.95,
  182 共享 fret)。**深度線自己補刀:minx 在 t=1500-1800 間打到 2.49 —
  正是軍隊的梯級**(run17 彈道紀錄,同一個量化節點):移民花 25 分鐘到達
  軍隊 30 秒到的地方 — v9 分的不是目的地,是速度。**Law v12:憲法立法的是
  節奏與裝訂,不是領土** — 同一個縫核正典、同一批深梯級、同一個閱讀順序
  (93% 跨劑量、100% 同法巢套);劑量只決定同一本書讀多快、以及多少讀不到
  的外圈被裝訂進印刷。每次修法都把權力從法移向格。開國計數 N=8:54×4/55/
  57/58/57(今晚環境下疑似重新收斂於 57);幾何照舊 digit-exact(minx
  8.0570),但開國 shareA 首見第四位小數擺動(0.755 vs 0.753×2)。
  (f9) ✅ 絕對正典 pass(2026-08-25 凌晨,第二十三拍,measure22.py +
  analyze22.py,雙 run:0.80 復跑 3600 + 1.00 補 1800)— **絕對正典死了,
  死出比它更強的東西**。P72 falsifier 命中:0.80@3600 lit 181(73.0%),
  穿破 [150,168] 天花板 — run19 的休會(154)是**旱季不是死亡**(讀取到最後
  仍陣發:2400-3000 只 +1,3000-3300 +5)。至此本作記錄過的每一次「死亡」
  都在更長的窗裡復活 — **從來沒有議會被觀測到死過;休會偵測器正式退役**
  (旱季與死亡在任何有限窗裡不可分)。P73 誠實中間帶(預寫過):1.00@1800
  lit 118,drip 還在流 — 絕對問題延判;但 share 76.1% = 史上最高,且
  **1.00 挖出全作史上最深 meeting:x=1.18 @ t=900,B 的爐邊本身**(6 顆
  hearth node 點亮,舊紀錄 2.17);0.80 也在 t=3300 打到移民紀錄 1.98 —
  **pass 21 的「時間只買廣度不買深度」也死了**:時間買得到深梯級,只是用
  旱季付款。(誠實 caveat:run B 開國落在**另一個吸引子** 65/15/7.15,
  西偏 — 爐邊紀錄可能是開國鍵控的,復跑排入 f10。)P74 雙條款全過:
  同法 lit-set 新骰子重現 99%(152/153)/ 100%(99/99);跨法 90%
  (200 共享 fret);暗巢套 99%/100%。P75 分裂:格的鑄造 248/248、155/155
  **位置逐字重現**(第四、五次決定論確認 — 鑄造層完全決定論);零錯、
  fps 90/104;但開國計數一夜連破兩次帶(60、65;N=10:54×4/55/57/58/57/
  60/65,單調向上 — 環境項真實,照 lesson 9 記帳不追),開國 shareA
  穩守 0.755/0.753。**Law v13:正典是序列,不是集合** — 一條閱讀順序屬於格
  (新骰子下 99-100% 位置重現);每部法把這條序列的不同長度裝訂成書、以
  自己的節奏沿著讀;且**閱讀永不結束** —「停滯」「份額」「死亡」全是
  age × tempo 的照片,同一個幻覺本作已在五個層抓到五次。存檔好奇非定律:
  0.80 與 0.90 在 3600 同讀 73.0% — 但 1.00 在半歲已 76.1%,share 不是
  純時鐘,薄書跑在前面。
  (f10a) ✅ 開國普查 pass(2026-08-25 凌晨,第二十四拍,同夜第二口,
  measure23.py,8 次開國 × 300s 各自新瀏覽器,無政變)— **P77 falsifier A
  命中即發現:8/8 全落 8.0570,四位小數,7.15 盆地一次都沒出現** —
  jitter-stochastic 死:乾淨條件下開國**根本沒有骰子**。7.15 只在重負載下
  出現過(measure16 的 5s tick;昨拍 run B 前面壓著一小時連續瀏覽器馬拉松)
  → 雙峰是**機器狀態選的**。Lesson 7 磨利:量測者的節拍器是 seed 的一部分,
  現在加上 — **量測者的重量是控制輸入**;負載不加 variance,負載選一條
  不同的決定論分支。P76 8/8 過(minx@60 == minx@300,分支第一分鐘定案)。
  P78 懸置(無 7.15 樣本)但計數自己說完了話:普查band 50-58,兩端都破
  舊帶(54 的地板站了 N=10,普查給出 53 和 50)—「54 地板」跟三/五同屬
  小 N 命理。計數層終審:噪聲純量,均值 ~55,±15%,不載結構。P79 技術性
  雙端違反:shareA [0.745, 0.765] vs 預測 [0.748, 0.760] — share 是 1% 粗
  常數,pass 22 的「第四位小數擺動」筆記本身也是命理。P80 8/8 零錯。
  **開國三層三種本體論:幾何 = 四位小數決定論、分支量化、負載選支、t=60
  定案;計數 = 純量測噪聲;share = ±1% 床常數。**爐邊紀錄的後果:1.18 是
  在 7.15/西偏開國上簽的,而普查說 7.15 需要負載 — 全作史上最深的音樂
  可能有機器熱狀態的共同署名。
  (f10b) ✅ 爐邊 de-confound pass(2026-08-26 凌晨,第二十五拍,measure24 +
  analyze24,單一 session-first 乾淨 run,1.00×1800)— **熱狀態共同署名
  無罪開釋:爐邊答的是法**。預註冊時檔案先開口:run17(東開國)與
  run22-B(西開國)在 t=600 同讀 2.4922 — 政變 600 秒內抹掉分支。實跑:
  P81 過(開國 8.0570 精確、count 57、share 0.755 — 第六次無骰子);
  P82 核心過 — **t=900 精確落 1.1786 四位小數、同時鐘、同 6 顆爐邊節點**,
  西開國的頭程優勢到 t=900 一絲不剩(子條款誠實記:t=600 中途讀數 2.4419
  非預測的 2.4922 — 連同分支的中途點都會 run-to-run 擺動:**中途點是天氣,
  終點是法**);P83 比預測更強 — 跨分支 lit/dark 一致 97%(151/155)且殘差
  是**純子集**:乾淨 run 零獨有格,負載 run 只是同一本書多讀 4 頁(三頁在
  遠東緣 x 20.5-21.1);run17 的 99 格 ratchet 再跑 100%。**同一本書、
  同一個順序 — 負載 reader 只是同齡多前進 4 頁**(存檔好奇非定律,54 地板
  的教訓:負載買不買閱讀速度要 N>1);P84 過 — 鑄造 155/155 位置全同
  (第六次決定論確認)、零錯、fps 81。**Law v13 磨利:開國是天氣,閱讀是法**
  — 機器狀態唯一摸得到的是你拿到哪篇序言;書、順序、最深的一頁、寬容法
  讀它們的時鐘,全是格的財產。
  後繼開口(2026-08-26 01:30 改向,Jie 對話 — 判準:研究要「帶得走」或
  「看得見」,詳 vault charter §2026-08-26 補記):
  **(f11 量測版取消** — 又一次吸收問題;25 拍的量測線收線)。
  **顯影 pass 完成(兩夜兩 commit)**:
  - **Part 1(08-26 `e94e9bf`)**:化石層帶頁碼(readIdx/litT,只進顯示與
    儀器,動力學不讀)— 地層漸層(早頁沉、晚頁亮 = Law v13 疊成 varves)、
    新沉積 ~45s 冷卻、初讀 flare(爐邊頁 x<1.5 最大最久);score() 輸出
    readIdx(作品自己記閱讀順序)。
  - **Part 2(08-27 `75bd9da`)**:四筆帳結清 — (1) **法/天氣分界被「演出」**:
    第一次動 γ/Δ = 憲政時刻,之前讀的頁永遠留 slate 紙(天氣管轄),之後
    是玉色法頁;沒動過旋鈕時整本書都是天氣(誠實)。(2) **護城河帶**:P35
    儀器教訓十一拍後進 HUD — census bar 從二分線改三段,爭議地帶 = 兩家園
    之間的暗段(暗因為它就是不確定)。(3) **地層振幅在老化場上判過,判出
    真失誤**:part 1 地板 s=0.55 讓第一頁比未讀 fret 還暗(讀過的頁沉到
    notation 層以下)— 地板拉到 0.75、頁尺寸壓過 ghost;mesh 場上閱讀
    現在是可讀的頁列。(4) **爐邊 flare 分級判定(標明 staged)**:野外
    1000s 沒等到爐邊(minx 卡 2.8455 — 10s polling cadence 是另一種天氣,
    pass 16 早知道 cadence 是 seed 的一部分),labeled injection 在真爐邊
    節點(x=-0.03)判:峰值蓋過全場所有 organ/spring/spark、玉色非聲部色、
    冷卻後讓位日常交通 — 「稀有讀起來像稀有」成立;野外捕捉留作 standing
    pleasure 非欠帳。儀器記錄:swiftshader 截圖整晚全白(N=3,part 1 的
    one-off 是預警)→ canvas.toDataURL 才是可靠 readback;GPU 渲染(觀者
    真正看的)判顯示,108-113 fps。
  **✅ 封版(2026-08-28 00:3x-01:1x,第二十七拍 = 最後一拍)**:新鮮眼睛
  互動走完 — untouched(整本書=天氣,constT null 驗證)→ 憲政時刻(Δ 第一觸,
  constT=247.8 即刻落章)→ 滑移(泉 wandering)→ 脊遷都(修剪+入侵直接可見,
  shareA 0.838,玉綠大教堂深入 B 腹地)。120fps、零 JS 錯零警告、audio 醒、
  憲法分頁真實(68 slate 天氣頁 / 45 玉法頁)。收版手勢自己送上門:走的路上
  minx 落 **2.1706 = run16 的史上最深租約梯級**(2.17,B 的爐邊街)— 隨手的
  訪客走法也踩在格的量化梯級上,梯級是床的財產不是協定的財產,最後一晚作品
  又自證了一次 Law v13。乾淨機器 1800s 野外爐邊不跑:bot 在線的 Mac mini
  永遠不是乾淨機器(lesson:量測者的重量是控制輸入),野外爐邊維持 standing
  pleasure 非欠帳。README 作品段 + root index 標記 + charter 收尾同 commit。
  此後不改;URL 永久。
  **006 種子(下一個真工作)**:聆聽宿主機器的作品(lesson 7 反轉 — 負載
  決定論地選分支,機器狀態從實驗誤差翻成命題;與 005 的關係 = 005 與
  003 的關係:繼承一個發現,翻轉它的角色)。

**piece 004 — coupled metronomes**(first light 2026-07-29)。
`pieces/004-coupled-metronomes/index.html` — 承 003 同床同 seed(刻意:唯一改變是耳朵的規則,對比顯影)。

- **命題**:003 的兩聲部從沒聽見彼此(Δ 是外加的,縫的遊走是拍頻=算術)。004 給它們
  耳朵,且耦合通道只能是基質本身。**時鐘 → 相位振盪器**(φ∈[0,1) wrap 發射),
  type-II PRC(Δφ = −K/2π·sin2πφ·e),K 滑桿 = 聽力;**K=0 完全退化成 003** —
  前作活在這支的滑桿最左端。
- **First light 三個量測發現(measure.py,預測先寫後量)**:
  1. **P1 精確驗證**:聾態 ψ 漂移 −0.74 cyc/22s vs 理論 −0.76。
  2. **設計的耳朵一次都沒響**(mouth-node 抵達;E_DECAY 0.88^24≈0.046 死在 24 格
     場寬之前 — 耦合通道結構上不可達)→ 改成 **web ear(領土即聽覺器官,蜘蛛網式)**:
     對手鋒面敲你佔領的節點 = 撥你的網,~3 hears/beat,通道活了。縫從疤變耳朵。
  3. **探測範圍內(K≤2, Δ≥0.05)無鎖相**,且滑移率看起來等於或**快於**聾態。
     兩個假說未決:(a) slip 計數器 ψ-wrap 邊界抖動重複計數;(b) **真排斥** —
     邊界傳輸延遲 ≈4.6s ≈ 1.9T 翻轉有效耦合符號,「聽見彼此反而推開彼此」。
- **07-29 03:30 第二版 — 判決(measure2.py,unwrapped-ψ 漂移儀)**:兩假說各對一半。
  K=0 漂移 −0.0379 = 理論到小數四位(儀器可信);sign=−1 K↑ → |rate| 單調升
  (+9% at K=2)= **真排斥**;sign=+1 翻成吸引(−0.0364)。但效應僅 ±7% — 舊
  slip 計數主要是 wrap 抖動,疊在微弱真排斥上。
- **⭐ 弱的原因 = 本拍最大發現:多路徑色散**。border 撞擊來自不同路長的鋒面、
  散佈整個週期,kicks 相位不相干互相抵消 — **mesh 不是延遲線,是殘響室**,
  節拍資訊在穿越中被毀。web ear 聽到的是嗡鳴不是節奏。
- **07-29 04:30 第三版 — P5 驗證 + 升級 + 鎖相達成(measure3.py)**:
  γ=1.6(tree)下同樣 K 耦合強 2-4×;hear 率幾乎不變(~1.6/s)— 從來不是
  聽幾次的問題,是抵達是否同調。**符號跟著地形翻**(mesh:−1 排斥;tree:
  −1 吸引)— 單路徑把 kicks 集中在單一相位 φ*,淨方向 = sign(sin 2πφ*):
  **地不只設定耳朵的音量,決定你聽見的是什麼**。K=4 → 漂移 95% 抵消;
  **K=6 → 完全鎖相**,且 meetings 加倍(8-10 → 16-20)— 縫回到匯口開唱,
  像 Δ=0 但是「掙來的」:由地協商,不是作者設定。K 滑桿已開到 6。
  整夜弧線:聾 → 殘響室 → 地形翻符號 → 鎖。
- **07-30 00:3x 第四版 — 顯影(measure4.py,5/5 PASS)**:命題量完但觀者看不見
  耦合 → 兩個顯影,都用既有渲染語言:(1) **tremors** — 每次 hear 事件一道細絲
  從被敲節點奔向 mouth(web ear 的神經訊號;K=0 全聾無絲,開 K = 看著耳朵醒來);
  (2) **ψ sparkline**(右上,30s 窗)— 相位差軌跡當儀表線。
  - ⭐ **本拍發現(截圖驗證器抓的,連環三處)**:「flat: locked」是連續耦合直覺,
    對這支是**錯的** — 脈衝耦合的鎖相是「**原地站立的鋸齒**」(兩拍之間 ψ 照常
    自然漂移,每個 kick 拉回來;齒還在,只是不再走路)。滑移 = 走路的鋸齒。
    鎖相不是靜止,是原地呼吸 — 協商從沒停,只是不再輸。同一個錯覺藏了三處:
    caption、PV2 斷言(spread 判準 → 改 unwrapped-ψ 走速)、**coupleLabel HUD**
    (spread 判準把量到的鎖相標成 slipping → 改「walk %」儀:100%=聾態走速,
    0%=停走=鎖;mesh 排斥態免費變可讀 — walk >100% =「聽見反而推開」)。
  - 煙測數據:deaf tremor=0 ✓ / K=6 走速 0.0085 vs 聾 −0.0379(78% 抵消)✓ /
    meetings 16 vs 聾態等效 10.5 ✓ / label 誠實 ✓。
  - 另仲裁:deaf 白截圖 = swiftshader 截圖層 flake(頁內 readPixels 52% 亮),
    非作品 bug。title/psicap 寬螢幕相撞已修(max-width 66vw)。
- **07-31 03:3x 第五版 — 「鎖在誰的速度上?」(measure5/6.py)**:第四版問「鎖是什麼
  形狀」(站立的鋸齒),這版問下一題。四個預測兩個被推翻,而兩個推翻就是本拍的發現。
  - ⭐ **鎖不是折衷,是加速**(PH 推翻)。K=4 Δ=0.10 鎖在 2.32s、K=6 Δ=0.05 鎖在
    2.28s — **比任何一方單獨時都快**(自然週期 2.40 / 2.64、2.52)。kick 帳算得清:
    兩聲部 kick 總和的**差**付掉失諧(那才是鎖,對帳吻合:1.047−(−0.513)=1.561
    vs 應付的 1.516),**共模**則是淨注入兩邊的相位 — 沒人要求的免費加速。
    在這支上聽見彼此不是零和:**它們把彼此催進第三個速度,誰的都不是**。
  - ⭐ **耦合把領土拉平**(PI 從根部推翻)。原假設:web ear ⇒ 領土大 = 耳朵多 =
    讓步多。聾態普查確實傾斜 34:13(快的那方多佔),但一開耳朵就走向均分甚至
    反超(26/21、20/27、23/24)— 發火率相等則佔領率相等,於是 hear 數幾乎完全
    對稱(34/34、32/33、36/36),我要獵的不對稱**根本沒機會形成**。耳朵不只同步
    時鐘,**它重分配土地**。
  - **鎖 ≠ 同步抵達**(M14)。K=6 Δ=0.05 是量到最緊的鎖(drift 0.0002),meetings
    卻掉到聾態以下(7 vs 10)— 鎖在一個讓兩者無法同時抵達的相位差上(P3 的非零
    延遲,現在有了聽覺後果)。**同意節拍,不等於同意重拍。**
  - 顯影:HUD 加 realised-beat 讀數(`beat 2.32s — faster than both`),只在開耳朵
    後出現。**儀器坑第二次同型**:先用 EMA,鎖態下間隔本身是鋸齒 → 把擺盪讀成數值
    (報 2.64、真值 2.52)。改最近 8 個間隔的定窗均值,並把發火時間戳存進 STATS,
    讓驗證器能重建**同一個窗**對帳(smoke: HUD 2.49 vs tail 2.489 ✓、K=4 2.32 ✓、
    聾態不顯示 ✓、120fps、零 JS 錯)。
  - 附帶:K=6 Δ=0.10 兩次跑出 2.45 / 2.52 — 近閾值 run-to-run 變動,P4 的間歇性
    從速度側看到的樣子。
- **下一步(004 封版問題)**:核心已齊 — 命題量測完整 + 顯影完成 + 速度歸屬已答。
  剩:(a) 聽感 pass(需活人:dyad 濃度、tremor 知覺強度、鎖相前後聽感對比,現在
  多一項:**鎖但不同步**的 Δ=0.05 K=6 聽起來是什麼);(b) 可選:Arnold tongue
  掃圖 — 非封版必要。傾向:活人 pass 後即可封。
- 驗證器已入 repo(`measure.py`–`measure6.py`,craft rule 7)。fps 120 全程,零 JS 錯。

**piece 003 — two metronomes, one bed**(2026-07-27 第一版;07-28 凌晨第二版)。
`pieces/003-two-metronomes/index.html` — 單檔 WebGL2 + Web Audio,seed 20260727。

- **命題**:干涉不是波的專利 — spiking network 上,**不應期就是相消**。
  兩聲部(A=ember/triangle、B=cyan/sine)各佔一端,共用中央匯口;
  meeting(同步抵達 → 白火花+dyad)vs shadow death(陰影內後到 → 暗火花+沉默)。
- **07-28 量測(20s/檔,STATS 累積計數器)— 回答了「Δ>0.2 視覺節奏」的開口**:

  | Δ | meetings | shadows | maxMeetGap |
  |---|---|---|---|
  | 0.10 | 4 | 30 | 2.44s |
  | 0.20 | 10 | 44 | 6.42s |
  | 0.35 | 9 | 38 | 6.79s |
  | 0.50 | 7 | 41 | 8.29s |

  **預測被推翻**:原以為高 Δ 會讓 meeting 崩掉(COINCIDE 窗只佔滑差的 18%)— 實際上
  meeting 率不降(mesh 路徑抖動製造額外對齊機會),但 **maxMeetGap 單調爬升,且 ≈ slip
  週期 2.4(1+Δ)/Δ**(Δ=0.5:8.29s vs 理論 7.2s)。**Δ 不殺縫,Δ 給縫寫樂句**:
  靜默…爆發群…靜默,拍頻結構直接變成 phrase structure。fps 全域 121,零 JS 錯。
- **07-28 定裝:shadow 記得殺手**。shadow spark 帶 `own`(誰的不應陰影殺的),渲染
  分色(死在 A 陰影=暗暖、死在 B=暗冷)— 長靜默期邊界唯一的墨就是 shadow,分色後
  遊走斷層線不用一個音就可讀。功能已驗(own 流通、分支渲染);**知覺強度未驗**
  (JPEG 靜幀看不出淡火花 — 與聽感同桶,需要活人看)。
- **另收:高 Δ 領土大擺盪**(截圖發現)— B 週期長 35% ⇒ A 靜默窗裡 B 鋒面長驅直入
  半場,A 再開火推回。邊界事件統計上守在匯口(meanX~11, sd~2.7)但鋒面滲透很深 —
  「事件邊界穩定、佔領邊界擺盪」是兩層不同的邊界,作品免費呈現了這個區分。
- **未驗(不變)**:實際聽感(dyad 濃度/兩聲部平衡/polyrhythm 端聽感)、真機觸控、
  分色 shadow 的知覺強度。皆需非 headless 環境。

**piece 002 — carved bed as instrument**(完成,07-24~26)。刻床當樂器:結構=錄音、
脈衝=播放,γ=音色。γ 兩極端已驗證為作品真相。行動裝置結構驗證完。

**piece 001 — self-channelling flow**(已封版 2026-07-26)。命題由 002 接走。URL 永久。

## craft 記錄(跨作品)

1. 色溫是最便宜的層次分離手段(001→002)。
2. refractory 是把爆炸變樂句的那顆參數(002)— 003:同時是相消帶寬度,佔比決定邊界活不活。
3. 同參考系的量要一起縮(002)。
4. 極端值不是 bug,是作品的兩個真相(002)。
5. 多模態作品的 QA 要多模態(002)。
6. (003)參數不跨命題繼承:換命題先重推參數的角色。
7. (003)驗證裝置化:debug hook + Playwright 定量取樣。**07-28 升級:瞬時陣列(有 life
   的 sparks)不夠,要累積計數器(STATS + reset())— 低頻事件在快照瞬間常常正好不在。**
8. **(003 新)量測前先寫下預測**:高 Δ 量測推翻了「meeting 會崩」的直覺 — 參數的作用
   要量了才知道是「殺死」還是「重組」。預測寫在前面,推翻才有記錄價值。
9. **(003 新)資訊已在系統裡,只是沒顯影**:shadow 的 owner 本來就存在,加一個欄位一個
   渲染分支,靜默期就有了敘事。找「已存在但未顯影的資訊」比發明新層便宜且誠實。
10. **(004 新)短記憶估計器會把擺盪讀成數值**:第四版 ψ-spread 把鋸齒的振幅當成滑移,
    第五版 EMA 把鋸齒的擺盪當成週期 — **同一個坑,兩種面孔**。凡是量一個「本身就在
    週期性擺盪」的東西,估計窗必須長過那個週期。判準:先問「這個量在穩態下是常數還是
    鋸齒?」是鋸齒就別用瞬時值或短 EMA。
11. **(004 新)驗證器要能重建 UI 用的同一個窗**:HUD 讀數與全程均值不一致時,無法判斷
    是儀器錯還是尾段真的變了。把原始事件時間戳存進 STATS,驗證器就能算出同窗值直接對帳
    (本拍:HUD 2.49 vs 同窗 2.489,確認儀器誠實、差異來自尾段漂移)。
12. **(005 新)選窗之前先問系統會不會老化**:對非平穩(會老化)的系統,「X 還會不會
    發生」的答案由窗的**位置**(床齡)決定,不是窗的**長度** — N 個年輕短窗 ≠ 一個
    長窗。規則 10 管「窗要長過振盪週期」,這條管更上游:振盪假設本身可能不成立。
    判準:把事件率對時間畫出來,前 10% 窗口佔掉大半事件 = 開國期現象,加窗無效。
13. **(006 新)信任感測器之前,先比「訊號的預期變化量」與「量測量子」**:
    牆(OS 的保護)與刻度(計時解析度)都是儀器事實,日常訊號可能整個
    活在兩者之間的縫裡 — 飽和測試過了不代表日常可見(the hand ≠ the day)。
    判準:量測值的 p95 若恰好落在一個量化階上,看到的是尺不是世界。

## 下一步(候選,未定)

- **003 收尾清單(需活人/真機)**:聽感 pass、分色 shadow 知覺強度、真機觸控。
  headless 能做的都做完了。
- 池中剩餘候選:
  1. ~~耦合的節拍器~~(→ 004 動工 2026-07-29,第五版近封版)
  2. ~~和弦作為拓樸~~(→ 005 動工 2026-08-01,進行中:十二 pass 至 08-16 — 打擊層 / 領土 / 遷都 / 回程 / soak 自反證 / biography sweep;現行定律 v3:肉的記憶 = 時間常數 + 履歷偏置。敘事見作品頁 journal + commit log)
  3. **遊走的匯**:sink 緩慢移動 →「猶豫—決定—固化」(從 003 讓位,仍在池裡)。
- 託管:GitHub `jieliou/substrate` + substrate.jieliou.com(CF Workers static assets)。
